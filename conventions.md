# 볼트 작성 규칙

## 폴더 구조

```
vault/
├── \\\_private/              ← .gitignore 대상, Git 미포함
├── projects/
│   └── {project-name}/
│       ├── code-lookup/
│       │   ├── web/       ← 프론트엔드 전용 (선택)
│       │   └── api/       ← 백엔드 전용 (선택)
│       ├── code-guide/
│       │   ├── web/       ← 프론트엔드 전용 (선택)
│       │   └── api/       ← 백엔드 전용 (선택)
│       ├── domain/
│       ├── troubleshooting/
│       │   ├── web/       ← 프론트엔드 전용 (선택)
│       │   └── api/       ← 백엔드 전용 (선택)
│       ├── installer/     ← 설치 스크립트 전용 (선택)
│       └── meetings/
├── shared/
│   ├── troubleshooting/
│   └── meetings/
└── templates/
```

### `\\\_index.md` (자동 인덱스)

* 각 프로젝트 루트에 `\\\_index.md` 파일을 두어 전체 문서 목록과 한줄 요약을 관리
* **갱신 시점**: "옵시디언에 정리해줘" 실행 후, 해당 프로젝트의 `\\\_index.md`도 함께 갱신
* **형식**:

  * 헤더: `> 최종 갱신: YYYY-MM-DD | 문서 N개`
  * 폴더별 테이블: `| 문서 | 한줄 요약 | 핵심 키워드 |`
  * meetings 폴더: `| 문서 | 날짜 | 한줄 요약 |`
  * 루트 파일(버그\_목록.md 등)은 별도 섹션으로 분리
* **링크**: Obsidian wiki-link (`\\\[\\\[파일명]]`), 서브폴더 파일은 `\\\[\\\[web/파일명]]`
* **한줄 요약**: 문서의 핵심 내용을 한 문장으로 압축
* **핵심 키워드**: LLM이 관련 문서를 빠르게 판별할 수 있는 3\~5개 단어

### 폴더별 역할

|폴더|용도|판단 기준|예시|
|-|-|-|-|
|`code-lookup/`|코드 읽을 때 찾아보는 것|"이게 뭐지?"|필드명 약어, 상태코드, 기술스택, 인증구현|
|`code-guide/`|코드 쓸 때 따라하는 것|"어떻게 하지?"|공통훅 가이드, 개발환경 설정|
|`domain/`|순수 업무 지식|"이 사업이 뭐지?"|도메인 해설, 업무 프로세스, 기획 변경이력|
|`troubleshooting/`|문제 해결 기록||버그 원인 분석, 해결 과정|
|`installer/`|설치 스크립트 전용|"설치/배포 관련?"|설계, 이관 가이드, 버그 기록|
|`meetings/`|회의록||스프린트 회의, 기획 미팅|

* 프로젝트 관련 문서는 `projects/{project-name}/` 하위에 작성
* 프로젝트 무관 범용 문서는 `shared/` 하위에 작성

### 프론트/백 분리 규칙 (code-lookup, code-guide, troubleshooting)

* 한쪽에만 해당하는 문서 → `web/` 또는 `api/` 서브폴더에 배치
* 양쪽 공통 문서 → 상위 폴더 루트에 배치 (섹션으로 구분)
* 서브폴더는 필요할 때만 생성 (문서가 없으면 폴더도 없어도 됨)

### 민감 정보 관리 (`\\\_private/`)

* SSH 비밀번호, Jasypt 키, 서버 IP 등 민감 정보는 `\\\_private/` 폴더에 저장
* `.gitignore`에 포함되어 Git에 올라가지 않음
* 파일명: `{project-name}\\\_credentials.md` (예: `erut\\\_credentials.md`)
* 원본 문서에서는 `\\\[\\\[\\\_private/{project-name}\\\_credentials#섹션]]` 링크로 참조
* 개발용 테스트 계정은 민감 정보가 아니므로 원본에 그대로 기재

## 폴더명

* 영어 소문자, 하이픈(`-`) 구분

## 파일명

|문서 유형|파일명 형식|예시|
|-|-|-|
|회의록|`YYYY-MM-DD\\\_주제.md`|`2026-03-13\\\_스프린트회의.md`|
|트러블슈팅|`주제.md`|`API호출\\\_타임아웃\\\_해결.md`|
|코드 조회|`주제.md`|`필드명\\\_약어.md`, `기술스택.md`|
|코드 가이드|`주제.md`|`공통훅\\\_사용\\\_가이드.md`|
|도메인|`주제.md`|`책무관리\\\_도메인\\\_해설.md`|

* 구분자는 언더스코어(`\\\_`)로 통일
* 파일명은 한글, 폴더명은 영어

## 태그

### 기술 태그

* 기술/도구 단위: `#javascript`, `#git`, `#css`, `#wijmo` 등
* 프로젝트를 넘어서 같은 기술 관련 문서를 모아볼 때 사용

### 상태 태그 (트러블슈팅 전용)

* `#미해결` — 아직 해결 못한 이슈
* `#해결완료` — 해결된 이슈

## 버그 목록

* 프로젝트 루트에 `버그\\\_목록.md` 단일 파일로 관리
* 체크리스트 형태로 작성, 수정 완료된 항목은 삭제
* 형식: `- \\\[ ] 버그 설명 — 상세 내용`

## 문서 내용

* 트러블슈팅/코드 가이드에는 반드시 Why(왜)와 Do(어떻게) 포함
* 프로젝트별 줄임말은 `code-lookup/` 하위에 용도별 분리하여 표 형식으로 관리 (예: `디렉토리\\\_약어.md`, `필드명\\\_약어.md`)
* 각주(`\\\[^1]`)를 활용하여 본문 흐름을 끊지 않으면서 부가 설명, 출처, 주의사항을 기록

## 다이어그램 (Mermaid)

* 관계도, 흐름도, 분류 트리 등은 ````mermaid` 코드블록으로 작성
* 파형, 격자, 단면도 등 Mermaid로 표현이 어려운 것은 ASCII 또는 이모지 사용
* 다이어그램 제목은 Mermaid 안에 넣지 말고, 바로 위에 **볼드 텍스트**로 작성 (subgraph 제목이나 제목용 노드는 겹침·시작점 혼동 발생)
* 노드 텍스트 안에서 `/`와 `숫자.` 사용 금지 — Mermaid가 마크다운으로 오파싱함. `/` → `·`, `1.` → `①` 사용
* 색상으로 상태 구분 시 권장 컬러:

  * 정상/완료: `fill:#90EE90` (연두)
  * 경고/참고: `fill:#FFE66D` (노랑)
  * 위험/에러: `fill:#FF6B6B` (빨강)
  * 정보/데이터: `fill:#87CEEB` (하늘)
  * 비활성/미사용: `fill:#E0E0E0` (회색)

## 지식 축적 범위

* 기존 대상: 코드 가이드, 도메인 용어, 트러블슈팅, 회의록
* 추가 대상: 대화 중 도출된 설계 판단, 트레이드오프 분석, 반복 질문 패턴
* "옵시디언에 정리해줘" 실행 시, 위 항목도 정리 대상에 포함
* 신규 문서 생성보다 기존 문서 갱신을 우선한다
* 배치: 기존 폴더 분류 기준(code-guide/, domain/ 등)을 따름

## 백링크

* 문서 작성·갱신 시 관련 문서가 있으면 본문에 \[\[백링크]]를 포함
* 예: code-guide 문서에서 관련 domain 문서 참조, troubleshooting에서 관련 code-guide 참조



